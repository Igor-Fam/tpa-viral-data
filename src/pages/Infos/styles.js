import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },

  title: {
    fontSize: 20,
    color: "#333",
    marginBottom: 3,
    fontWeight: "bold",
  },

  description: {
    fontSize: 15,
    color: "#333",
    marginBottom: 15,
    textAlign: "justify",
  },
});
